{
  "name": "setup-updater",
  "version": "1.0.0",
  "description": "GitHub Action to install mxcd/updater binary",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist --source-map --license licenses.txt",
    "test": "echo \"No tests yet\" && exit 0"
  },
  "keywords": [
    "github-actions",
    "updater",
    "setup"
  ],
  "author": "mxcd",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/tool-cache": "^2.0.1",
    "@actions/exec": "^1.1.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}
